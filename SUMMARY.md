# PixelPerfect PPT

Export your Figma Slides to pixel-perfect bitmap PowerPoint presentations.

This plugin exports slides as high-quality bitmap images embedded in PowerPoint format. The exported slides cannot be edited in PowerPoint, but this ensures perfect visual fidelity—every font, effect, and design detail is preserved exactly as designed.

Unlike Figma's built-in PowerPoint export which is editable but has rendering issues, this solution maintains visual accuracy while preserving PowerPoint's presentation functionality.

- Export all slides or only selected ones
- Adjustable quality settings from 1x to 4x
- Automatic slide ordering by page numbers
- Runs entirely locally with no network access

---

# PixelPerfect PPT

将 Figma Slide 导出为像素完美的位图PPT。

本插件将幻灯片导出为高质量位图并嵌入 PowerPoint 格式。导出的幻灯片无法在 PowerPoint 中编辑，但能完美还原设计——所有字体、效果和设计细节都能精确保留。

与 Figma 自带的 PowerPoint 导出虽然可编辑但存在渲染问题不同，本方案既保证视觉准确性，又保留演讲功能。

- 导出全部幻灯片或仅导出选中的幻灯片
- 可调节质量设置，支持 1x 到 4x
- 自动按页码排序保证顺序
- 纯本地运行，不访问网络
